# Copy to .env for local development if the backend isn't on the default
# http://127.0.0.1:8000. In Docker Compose this is injected as a build arg
# instead (see frontend/Dockerfile and docker-compose.yml).
VITE_API_URL=http://127.0.0.1:8000
